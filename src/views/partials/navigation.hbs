<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/">NodeTasks</a>
        <button class="navbar-toggler border-none" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item {{#if active.pages_home}} active {{/if}}">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item {{#if active.pages_about}} active {{/if}}">
                    <a class="nav-link" href="/about">About</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                {{#if user}}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {{user.name}}
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item {{#if active.notes_index}} active {{/if}}" href="/notes">All notes</a>
                        <a class="dropdown-item {{#if active.notes_create}} active {{/if}}" href="/notes/add">Add
                            note</a>
                        <div class="dropdown-divider"></div>
                        <button class="dropdown-item text-danger"
                            onclick="document.getElementById('formLogout').submit()">
                            Logout
                        </button>
                        <form action="/users/logout" method="POST" id="formLogout"></form>
                    </div>
                </li>
                {{else}}
                <li class="nav-item">
                    <a class="nav-link {{#if active.users_signin}} active {{/if}}" href="/users/signin">SignIn</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if active.users_signup}} active {{/if}}" href="/users/signup">SignUp</a>
                </li>
                {{/if}}
            </ul>
        </div>
    </div>
</nav>